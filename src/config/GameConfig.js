/**
 * Основные константы и настройки игры
 */
const GameConfig = {
  // Размеры игрового окна
  GAME_WIDTH: 1024,
  GAME_HEIGHT: 768,
  
  // Режим отладки (показывает хитбоксы и другую отладочную информацию)
  DEBUG_MODE: false,
  
  // Параметры игрока
  PLAYER: {
    SPEED: 120,          // пикселей/секунду
    HEALTH: 100,         // начальное здоровье
    ENERGY: 50,          // начальная энергия
    HITBOX_SIZE: 32,     // размер хитбокса (пиксели)
    ENERGY_REGEN: 5,     // восстановление энергии/секунду
    MAX_HEALTH: 100,     // максимальное здоровье
    MAX_ENERGY: 50       // максимальная энергия
  },
  
  // Параметры способностей
  ABILITIES: {
    ENERGY_SHOT: {
      COST: 10,           // стоимость энергии
      DAMAGE: 25,         // урон
      RANGE: 200,         // дальность в пикселях
      SPEED: 300,         // скорость снаряда
      COOLDOWN: 500       // перезарядка в мс
    },
    ENERGY_SHIELD: {
      COST: 20,           // стоимость энергии
      DURATION: 3000,     // длительность в мс
      DAMAGE_REDUCTION: 0.5, // блокировка 50% урона
      COOLDOWN: 8000      // перезарядка в мс
    }
  },
  
  // Параметры врагов
  ENEMIES: {
    DRONE: {
      HEALTH: 50,         // здоровье
      SPEED: 80,          // скорость
      DAMAGE: 15,         // урон
      ATTACK_RANGE: 150,  // дальность атаки
      ATTACK_COOLDOWN: 2000, // перезарядка атаки в мс
      DETECTION_RANGE: 120, // радиус обнаружения
      CHASE_RANGE: 200    // радиус преследования
    },
    ANOMALY: {
      HEALTH: 30,         // здоровье
      SIZE: 64,           // размер в пикселях
      DAMAGE: 10          // урон/секунду при контакте
    }
  },
  
  // Параметры ловушек
  TRAPS: {
    // Базовые настройки для всех ловушек
    DEFAULT_DAMAGE: 10,    // базовый урон для всех ловушек
    
    // Энергетические шипы
    ENERGY_SPIKE: {
      SIZE: 40,           // размер в пикселях
      DAMAGE: 15,         // урон при активации
      COOLDOWN: 3000,     // время между активациями в мс
      ACTIVE_DURATION: 1000 // длительность активного состояния в мс
    },
    
    // Лазерная сетка
    LASER_GRID: {
      SIZE: 48,           // размер центрального блока в пикселях
      DAMAGE: 20,         // урон при попадании в лазер
      COOLDOWN: 4000,     // время между активациями в мс
      ACTIVE_DURATION: 2000, // длительность активного состояния в мс
      BEAM_LENGTH: 120    // длина лазерного луча в пикселях
    },
    
    // Существующие ловушки
    BURNING_FLUFF: {
      SIZE: 48,           // размер в пикселях
      DAMAGE: 8,          // урон/секунду
      SLOW: 0.5,          // замедление (множитель скорости)
      SLOW_DURATION: 2000 // длительность замедления в мс
    },
    GRAVITY_TRAP: {
      SIZE: 64,           // размер в пикселях
      PULL_FORCE: 60      // сила притяжения пикселей/секунду
    }
  },
  
  // Параметры предметов
  ITEMS: {
    MAX_INVENTORY_SLOTS: 9, // количество слотов инвентаря
    HEALTH_PACK: {
      HEAL_AMOUNT: 40,    // восстановление здоровья
      MAX_STACK: 3        // максимум в инвентаре
    },
    ENERGY_CELL: {
      ENERGY_AMOUNT: 25,  // восстановление энергии
      MAX_STACK: 3        // максимум в инвентаре
    }
  },
  
  // Параметры генерации уровней
  LEVEL_GENERATION: {
    STATIC_FLOOR: {
      SIZE: 15           // размер первого этажа (15x15)
    },
    PROCEDURAL_FLOOR: {
      MIN_SIZE: 10,      // минимальный размер этажа
      MAX_SIZE: 20,      // максимальный размер этажа
      MIN_ROOMS: 15,     // минимальное количество комнат
      MAX_ROOMS: 25      // максимальное количество комнат
    },
    ROOM_TYPES: {
      EMPTY: 0.4,        // 40% вероятность пустой комнаты
      ENEMY: 0.35,       // 35% вероятность комнаты с врагами
      TRAP: 0.15,        // 15% вероятность комнаты с ловушками
      LOOT: 0.1          // 10% вероятность комнаты с предметами
    }
  }
};

export default GameConfig;